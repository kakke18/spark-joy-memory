{
  "name": "spark-joy-admin",
  "version": "1.0.0",
  "description": "spark-joy-admin === jsonからAjaxで読み込み、描画",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
